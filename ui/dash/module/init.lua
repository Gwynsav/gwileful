return {
   title  = require(... .. '.title'),
   user   = require(... .. '.user'),
   slider = require(... .. '.slider'),
   player = require(... .. '.player'),
   grid   = require(... .. '.grid'),
   random = require(... .. '.random')
}
